spring:
  application:
    name: global-station-review-service
  datasource:
    url: jdbc:mysql://localhost:3306/global_station_db
    username: root
    password:
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
    hibernate:
      ddl-auto: update


server:
  port: 8200
